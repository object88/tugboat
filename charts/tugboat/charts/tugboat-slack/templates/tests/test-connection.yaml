apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tugboat-slack.fullname" . }}-test-connection"
  labels:
    {{- include "tugboat-slack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tugboat-slack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
