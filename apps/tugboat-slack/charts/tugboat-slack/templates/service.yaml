apiVersion: v1
kind: Service
metadata:
  name: {{ include "tugboat-slack.fullname" . }}
  labels:
    {{- include "tugboat-slack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tugboat-slack.selectorLabels" . | nindent 4 }}
